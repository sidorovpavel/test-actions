name: New22

on:
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: commit-list-action
        uses: pavelsidorov/branches-difference-action@mian
        with:
          github-token: ${{secrets.TOKEN}}
          github-email: 'sidorovpav@ya.ru'
          jira-token: ${{secrets.JIRA_TOKEN}}
          jira-user: ${{secrets.JIRA_USER}}
          jira-domain: 'tangem'
          project-name: 'MM'
          release-version: 'test-4'
          release-file-path: 'test/versions22'
          release-file-prefix: 'abc'
